import React from 'react';
import Event from '../event/Event';

/** Legacy courses entry — opens unified hub filtered to Courses. */
const Courses = () => <Event initialCategoryType="course" />;

export default Courses;
